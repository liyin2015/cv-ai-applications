# Data
* data with label-> Supervised
* data without label-> unsupervised
* less data  -> few-shot learning
* no data, but have to achieve a certain task -> zero-shot learning
* data added continuously -> continual learning
# Model
* CNN
* Transformer
* Encoder-Decoder
* GAN
